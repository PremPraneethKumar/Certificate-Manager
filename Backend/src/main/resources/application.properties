# Set the application name
spring.application.name=certificate-manager

# MongoDB Connection
# (This connects to a local MongoDB running on the default port)
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=certificate_db

# Set the server port (optional, default is 8080)
server.port=8080